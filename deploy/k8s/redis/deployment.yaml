apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: aviron
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
              name: redis
          # No PersistentVolume — redis-room-registry.md's ownership keys
          # and evicted-user set are both self-healing via TTL, so losing
          # in-memory state on a pod restart is an accepted, bounded-impact
          # event, not data loss the way losing Postgres would be.
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          # Halved from 50m/64Mi requests, 200m/128Mi limits.
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
