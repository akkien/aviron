# Current Feature

## Status

Not Started

## Goals

<!-- What does success look like for this feature? -->

## Explain

<!-- Explanation of the feature/spec, populated by `load` -->

## Plan

<!-- Detailed implementation plan, architecture notes, tradeoffs -->

## Notes

<!-- Additional context, constraints, or details from spec -->

## History

- **Project Scaffolding & Local Postgres** (2026-07-18) — Go module (`github.com/akkien/aviron`), package layout (`cmd/server`, `internal/config|db|httpserver`), local Postgres via Docker Compose (`postgres:18-alpine`) with the initial schema migration (`golang-migrate`), `GET /healthz`, `.env`-based config (`godotenv`), and `make run`/`make test`. See docs/feature-log.md for details. Next up per the roadmap (context/project-overview.md §12, Phase 1): auth (`POST /auth/register`, `POST /auth/login`).
- **User Registration** (2026-07-18) — `POST /auth/register` (bcrypt cost 12, `409 email_taken`, `400` field-keyed validation errors), introducing the layered `handler → service → repository` architecture per domain (documented in context/coding-standards.md, "Backend Architecture") plus `internal/postgres` for driver-specific repos and `internal/httpx` for shared response helpers. Process wiring was reorganized mid-feature: `cmd/server/main.go` now only loads config and calls `app.Run(cfg)`, with DB setup and route registration moved into `internal/app.go` and `internal/httpserver/route.go`. Also planned the rest of Phase 1 into small specs under context/features/ (auth, races, frontend-client) with an index at context/features/phase-1-plan.md. Known cosmetic gap, not blocking: `internal/app.go` declares `package internal` directly instead of living in its own `internal/app/` subdirectory, so `main.go` needs an import alias (`app "github.com/akkien/aviron/internal"`) — worth tidying next time that file is touched. Next feature per context/features/phase-1-plan.md: `auth/login`.
- **Swagger API Docs** (2026-07-18) — Documented `GET /healthz` and `POST /auth/register` with `github.com/swaggo/http-swagger` annotations, generated `backend/docs/` via `swag init`, and served the UI at `GET /swagger/`. Added a `make docs` target to regenerate after future annotation changes. Not part of the Phase 1 feature plan — a standalone infra addition, branched and merged the same way.
- **Login & JWT Issuance** (2026-07-18) — `POST /auth/login` (bcrypt verify, HS256 JWT with `sub`/`email`/`exp` claims, 24h expiry), extending `auth.AuthRepository` with `GetUserByEmail` and collapsing "user not found"/"wrong password" into one `ErrInvalidCredentials` so the response can't be used to enumerate accounts. Renamed the domain's types to match `internal/postgres`'s existing convention (`Handler`/`Service`/`Repository` → `AuthHandler`/`AuthService`/`AuthRepository`, constructors `NewAuthHandler`/`NewAuthService`), moved all request/response DTOs into a new `dtos.go`, and documented both as a standing convention in coding-standards.md for future domain packages (e.g. `race` → `RaceHandler`/`RaceService`/`RaceRepository`). Also fixed a latent `.PHONY` bug in the Makefile that made `make docs` a no-op. **Verification gap carried into master:** `go build`/`go vet`/`gofmt -l .` are clean, but `go test ./... -race` was not run after the rename (skipped per explicit request) — the rename was mechanical with no logic changes, so risk is low, but this is not independently confirmed; run the full suite before building on top of this. Next feature per context/features/phase-1-plan.md: `auth/jwt-middleware`.
- **JWT Auth Middleware** (2026-07-18) — `internal/middleware.Auth(jwtSecret)`, verifying the JWT `Login` issues (HS256, `sub`/`exp` claims, explicit expiry check alongside the library's implicit one) and exposing the caller's user id via `UserIDFromContext`. Moved mid-feature from `internal/auth/middleware.go` (`auth.RequireAuth`) into its own top-level `internal/middleware` package (`middleware.Auth`), since the logic has zero dependency on `internal/auth`'s domain types — documented as a standing convention in coding-standards.md ("cross-cutting middleware gets its own top-level package"). Verified: `go build`/`go vet`/`gofmt -l .` clean, and all `TestAuth_*` tests pass (closing the verification gap the previous feature left open, at least for this package). Not wired into any route yet — nothing to protect until `races/create-race`. Also backfilled concise `docs/feature-log.md` entries for User Registration and Login & JWT Issuance, which hadn't been documented there yet. Next feature per context/features/phase-1-plan.md: `races/create-race`.
- **Create Race** (2026-07-18) — `POST /races` (authenticated), the first domain package beyond `auth` (`internal/race`: `RaceHandler`/`RaceService`/`RaceRepository`, `NewRaceHandler`/`NewRaceService`, `dtos.go`), following the layered convention exactly — deliberately diverging from the feature spec's older flat-function handler wording, which predates that convention. No new migration needed: `races` already had every column this feature uses from the scaffolding migration. This is the first route actually wrapped with `middleware.Auth`, closing the loop the JWT Auth Middleware feature left open. Verified: `go build`/`go vet`/`gofmt -l .` clean, and the *entire* test suite passed (`go test ./... -race`), including `internal/auth` — this also closes the verification gap the Login & JWT Issuance feature had left open. Manual curl/DB verification deferred until Phase 1 is fully done, per explicit request. Next feature per context/features/phase-1-plan.md: `races/join-race`.
- **Join Race** (2026-07-18) — `POST /races/{id}/join`, extending `internal/race` (not a new package) with `GetRace`/`AddParticipant`, three new sentinel errors (`ErrRaceNotFound`, `ErrAlreadyJoined`, `ErrRaceNotPending`), and a signed HS256 session token (`race_id`/`user_id` claims, 6h TTL) mirroring `AuthService`'s JWT-signing pattern — `RaceService` now also holds a `jwtSecret`. `{id}` UUID validation happens in the handler via a small regex, not a UUID library, consistent with this project's plain-`string`-ids convention. No new migration needed. The test suite itself caught a real bug: the fake repository's generated ids (`race-1`) failed the handler's own UUID-format check, exactly as it should have — fixed the fixture to generate UUID-shaped ids. Also formalized two workflow conventions learned this session directly into the `/feature` skill files: `start.md` now states verification (build/vet/gofmt/test) happens once and isn't repeated at `complete`, and housekeeping like `make docs` doesn't get its own tracked `TodoWrite` item. Verified with `go build`/`go vet`/`gofmt -l .`/`go test ./...` (no `-race` this run, per explicit request). Next feature per context/features/phase-1-plan.md: `races/start-race`.
- **Start Race, Prompt Text & Race Status** (2026-07-20) — Bundled two specs (`races/start-race.md`, `races/race-status.md`) into one feature since both extend `internal/race` and share rationale. Added `POST /races/{id}/start` (creator-only, generates prompt text via `gofakeit`'s `Word()` called `distance_meters` times for exact word-count control, flips status to `active`), `GET /races/{id}/text` (fetch the generated prompt, `409` if not ready), and `GET /races/{id}` (race + participants via two queries: race row, then `race_participants JOIN users`). New migration `000002_add_prompt_text` (`races.prompt_text` was only ever in the schema doc, not the real migration). Two new sentinel errors (`ErrNotCreator`, `ErrPromptNotReady`), deliberately not collapsed into the existing `ErrRaceNotPending` despite both being `409`s, since `ErrPromptNotReady` means the opposite condition. Diverged from `start-race.md`'s original "hardcoded word list" suggestion in favor of `gofakeit` — same no-network-dependency guarantee, better variety; confirmed via `go doc` that `gofakeit` v7's `Paragraph`/`Sentence` are deprecated no-ops, so looped `Word()` instead. Verified: `go build`/`go vet`/`gofmt -l .` clean, full `go test ./... -race` passed, Swagger docs regenerated. Design questions raised during review (not yet actioned): whether `CreateRace` should auto-add the creator as a participant (currently it doesn't — creator must call `join` like anyone else to get a `session_token`/race slot), and whether a minimum-participant guard should gate `start` (currently `start` is lobby-style: creator decides timing, no participant-count check). Next feature per context/features/phase-1-plan.md: `frontend-client/login-page`.
- **Frontend Client — Login Page & Create/Join Race Page** (2026-07-20) — Bundled `frontend/login-page.md` and `frontend/create-join-race-page.md` into one feature. Scaffolded `frontend/` (Vite + React + TypeScript, `yarn create vite`) styled with Tailwind CSS v4 (`@tailwindcss/vite`, CSS-based `@theme`, no `tailwind.config.ts`) and hand-authored shadcn/ui primitives (`Button`, `Input`, `Label`, `Card` — not run via the shadcn CLI, written by hand to match its standard output). Built `/login` (JWT storage in `localStorage`) and `/races` (create/join/start forms, a status view, and a typing view with per-word progress tracked entirely client-side, no WebSocket yet). Discovered and fixed a gap neither spec covered: the backend had zero CORS support, and every authenticated frontend request would have failed preflight — added `internal/middleware/cors.go` (wraps the whole mux, not per-route, since `OPTIONS` preflights never match `http.ServeMux`'s method-specific patterns) plus a `CORSAllowedOrigin` config field. Also added `make start`/`stop`/`restart` (build-then-run in the background with a PID file) to `backend/Makefile`, and a custom race-lane color palette (`lib/colors.ts`, 18 colors) plus a blue (`#3A59D1`) primary theme color, both per explicit user request after initial implementation — user caught that their own color list mislabeled two blue hexes as "green" and vice versa, confirmed the intended primary via a clarifying question before touching the theme. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean on the backend; `yarn build`/`yarn lint` clean on the frontend (one pre-existing, expected shadcn lint warning about `buttonVariants` co-export, unchanged from upstream shadcn's own component shape). Full REST chain (register → login → create → join → start → text → status) manually verified via curl with two users, plus a CORS preflight check — but not visually verified in an actual browser, since no browser automation tool is available in this environment; this is a known, disclosed gap, not a claimed pass. Per context/features/phase-1-plan.md, Phase 1 (Auth + Races + Frontend Client) is now fully complete. Next: Phase 2 (context/project-overview.md §12) — the room actor, WebSocket protocol, and reconnection handling.
- **Cap Race Participants at 10** (2026-07-20) — Small, explicitly-requested addition ahead of Phase 2: `internal/race` gains `MaxParticipants = 10` and `ErrRaceFull`, checked in `JoinRace` via a new `CountParticipants` repository method before `AddParticipant` (409 `race_full` once a race is full). Mainly motivated by Phase 2's room actor, which broadcasts every participant's state on every 250ms tick — an unbounded room means an unbounded per-tick payload. Accepts the same count-then-insert race-condition gap `start-race.md` already accepted for its ownership/status check (two near-simultaneous joins could push slightly past 10) — not worth a transaction/row lock for a side project. Updated `context/features/phase1/races/join-race.md` and added a note to `context/features/phase2/room-actor/room-actor-core.md` tying the cap to that feature's bounded-broadcast-size assumption. Verified: `go build`/`go vet`/`go test ./... -race` clean. Implemented directly on `master` (not through a `/feature` branch), consistent with how the CORS fix and Tailwind/shadcn stack update were handled — a small change explicitly requested outside the active `/feature load` cycle.
- **Room Actor Core** (2026-07-20) — First Phase 2 feature (`context/features/phase2/room-actor/room-actor-core.md`): new `internal/room` package — `RoomActor`, `ParticipantState`, and a `RoomEvent` sum type (`ParticipantJoined`, `TelemetryReceived`, `ParticipantDisconnected`) implemented as a Go interface + marker method rather than a flat tagged struct, since `reconnection/grace-period.md`'s later variants (`ParticipantReconnected`, `ParticipantLeft`) would otherwise force optional fields onto a shared struct. `Run()` ticks every 250ms via a `select` over `inbox`/ticker/`ctx.Done()`, broadcasting a ranked `race_state` snapshot (stable tie-break) as `[]byte` over a `chan<- []byte` — non-blocking send, so a full/slow consumer never stalls the actor. `applyEvent` mutates `r.participants` in place and is directly unit-testable without ever calling `Run()` (no goroutines needed), rejecting stale/duplicate/out-of-order telemetry by `Seq <= LastSeq`. Deliberately does **not** follow this project's Handler/Service/Repository layering (context/coding-standards.md) — that convention is for REST domains with a DB round trip, and this is an in-memory actor with none of that shape. The `race_state` JSON struct is defined locally in `internal/room` for now, anticipating `websocket/protocol.md` (not yet built) — flagged to reconcile when that feature lands, most likely by having it import `internal/room`'s types. One implementation-time deviation from the loaded plan: `tick` is an incrementing counter (`r.tickCount++`) rather than a wall-clock `UnixMilli()` timestamp, both for more conventional "tick" semantics and to make the broadcast test deterministic. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean, including 3 concurrency-focused tests (broadcast-on-tick, clean shutdown on context cancel with no goroutine leak, and concurrent senders on `inbox` from 5 goroutines × 50 events each) — re-ran 5x to rule out timing flakiness before considering it done. No WebSocket wiring, registry, reconnection, or finish-detection in scope here — those are separate, later Phase 2 features that depend on this one. Next feature per context/features/phase2/phase-2-plan.md: `room-actor/room-registry.md`.
- **Room Registry** (2026-07-20) — Second Phase 2 feature (`context/features/phase2/room-actor/room-registry.md`): `internal/room` gains `Registry` (`NewRegistry`/`Spawn`/`Get`/`Remove`) matching the spec's data shape exactly — a `sync.RWMutex`-guarded `map[string]*RoomActor`, single-instance-only (Phase 4 replaces this with Redis-backed ownership). Resolved a gap the spec itself left open (flagged during `load`/`start`): `Spawn`'s signature has no `broadcast chan<- []byte` param, but `NewRoomActor` requires one — solved by changing `RoomActor`'s `broadcast` field to a bidirectional `chan []byte` (created internally by `Spawn`) and adding an exported `Broadcast() <-chan []byte` accessor, so the not-yet-built `websocket/ws-endpoint.md` can read outgoing snapshots after a `Get` without `Spawn`'s signature needing to change. Wired the one hook the spec called for that's actually buildable today: `POST /races/{id}/start` now calls `registry.Spawn(...)` after a successful start. This required `RaceHandler` to hold both the `*room.Registry` and the process's **root** `context.Context` (not `r.Context()`) — using the per-request context would have cancelled the room actor the instant the HTTP handler returned, since `net/http` cancels a request's context on return; the root context is threaded from `internal/app.go` through `httpserver.RegisterRoutes` alongside the existing `pool` param. Deliberately put the `Spawn` call in the **handler**, not `RaceService`, despite `RaceService` normally holding orchestration logic — spawning an in-memory actor isn't a repository-backed business operation the way the rest of `RaceService` is, and threading a `*room.Registry` through `NewRaceService` would have forced an unrelated constructor-signature change onto all ~15 existing `RaceService` unit tests for a concern those tests don't exercise; the handler-level hook only touched the 2 actual `NewRaceHandler(...)` call sites. `Remove` was **not** wired into `race-completion/finish-race.md` or `reconnection/grace-period.md` — neither exists yet, exactly as flagged during `load`. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean, including the two concurrency scenarios the spec explicitly required (concurrent `Get` during `Spawn`, `Remove` racing an in-flight `Get`, both across 50 races) — re-ran the `internal/room` package 3x to rule out timing flakiness. Next feature per context/features/phase2/phase-2-plan.md: whichever of `websocket/ws-endpoint.md`, `race-completion/finish-race.md`, or `reconnection/grace-period.md` is next in that plan's ordering.
- **WebSocket Protocol** (2026-07-20) — Third Phase 2 feature (`context/features/phase2/websocket/protocol.md`): new `internal/ws` package (not `internal/websocket`, to avoid a future name clash with whichever WS library `websocket/ws-endpoint.md` adds), pure JSON encode/decode with no Handler/Service/Repository layering — same reasoning as `room-actor-core.md`. Closed the reconciliation gap that feature's history entry flagged: `internal/room`'s private `raceStateMessage`/`participantSnapshot` structs are now exported in place as `RaceStateMessage`/`ParticipantStateJSON` (rename only, no behavior change), so `internal/ws` reuses them instead of redeclaring an identical shape — keeps the dependency one-directional (`ws` → `room`; `room` doesn't need to know about `ws`). Resolved the design question flagged during `load`/`start`: since the wire schema's `join_race` message only carries `race_id` but `room.ParticipantJoined` also needs `DisplayName`, `decodeClientMessage([]byte) (ClientMessage, error)` only parses/validates the envelope (known `type`, valid JSON) as literally specified, and a separate unexported method, `ClientMessage.toRoomEvent(userID, displayName string) (room.RoomEvent, error)`, does the `type`-dispatch into `RoomEvent` variants — taking identity as caller-supplied params since the future `ws-endpoint.md` will already have them from the verified session token. Also deliberately did **not** add an `encodeRaceStateMessage` despite the spec listing "one function per outbound type": `RoomActor.broadcastSnapshot` (from `room-actor-core.md`) already marshals and hands out pre-encoded `race_state` bytes over `Broadcast()`, so a second encoder for the same JSON shape would be unused dead code today — only `encodeRaceFinishedMessage` was added, since `RaceFinishedMessage`/`RaceResultJSON` don't exist anywhere yet (`race-completion/finish-race.md` isn't built). Tests live in `package ws` (not `ws_test`), matching `internal/room`'s established convention for same-package testing of unexported pure-logic functions. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean across the whole module, including the new `internal/ws` tests (decode success/failure paths for both message types, both `RoomEvent` conversions, and the finished-message encode round-trip) and confirming the `internal/room` rename didn't break its existing tests. Next feature per context/features/phase2/phase-2-plan.md: `websocket/ws-endpoint.md`.
- **WebSocket Endpoint** (2026-07-20) — Fourth Phase 2 feature (`context/features/phase2/websocket/ws-endpoint.md`): `GET /ws?race_id=...&session_token=...`, added to `internal/ws` (not a new package) alongside the protocol layer. Added `github.com/coder/websocket` as the WS library per explicit request (project-overview.md §11 left either `gorilla/websocket` or `nhooyr.io/websocket` acceptable; `coder/websocket` is `nhooyr.io/websocket`'s renamed continuation) — its context-native `Read`/`Write` fit this project's context-driven goroutine style better than `gorilla/websocket`'s deadline-based cancellation. Resolved every gap flagged during `load`/`start`: (1) **fan-out** — `RoomActor.Broadcast()` is one channel per room, but each connection needs its own buffered channel, so a per-room `hub` (new `hub.go`) fans broadcasts out; refined mid-implementation from the sketched mutex-guarded map to a channel-driven single-writer design instead (documented in the new `docs/concurrency.md`, added this feature per explicit request after a design-rationale discussion) — no mutex on the hot path, only `hubRegistry`'s lazy-create bootstrap needs one; (2) added `RoomActor.Context()` and `RoomActor.Send(ev RoomEvent)` accessors to `internal/room` (`Send` guards against blocking forever post-shutdown via a `select` on `ctx.Done()`, the same reasoning `hub.registerConn`/`unregisterConn` reuse against their own `closed` channel); (3) `displayName` falls back to `userID` (no DB round-trip, honoring the spec's "no new Postgres access" literally); (4) `applyEvent`'s `ParticipantJoined` case now calls `broadcastSnapshot()` immediately, satisfying protocol.md's "immediate snapshot on join" requirement that nothing had implemented yet; (5) the reader goroutine's read-error exit path pushes `room.ParticipantDisconnected` via `Send`, wiring up an event type `room-actor-core.md` had already built but nothing fed yet. `WSHandler.ServeHTTP` runs no Handler/Service/Repository layering (no DB round-trip) and is registered directly as `GET /ws`, deliberately outside `middleware.Auth` since it authenticates via the query-string `session_token`, not the `Authorization` header — reuses `cfg.CORSAllowedOrigin` for `coder/websocket`'s `OriginPatterns`, since without it the frontend's cross-origin handshake would be rejected by default (a gap neither this spec nor the original CORS work had flagged). A `wsConn` interface lets most tests run against a fake connection (`fake_conn_test.go`) instead of real sockets — one real end-to-end suite (`endpoint_integration_test.go`) still dials a real `coder/websocket` client against `httptest.Server` for the happy path and all three rejection cases, proving the wire-level integration actually works. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean across the whole module (`go mod tidy` after adding the dependency); `internal/ws`/`internal/room` re-run 5x with no flakiness, matching this project's concurrency-feature verification bar. Not yet wired: `reconnection/grace-period.md`'s grace-period timer (the disconnect event is now sent, but nothing times it out yet) and `race-completion/finish-race.md` (no `race_finished` message is ever actually sent). Next feature per context/features/phase2/phase-2-plan.md: `reconnection/grace-period.md`.
- **Reconnection & Grace Period** (2026-07-21) — Fifth Phase 2 feature (`context/features/phase2/reconnection/grace-period.md`). Two open design decisions were resolved *before* `start` (with the user, across a `load`-time design discussion) and the spec files updated on disk to match, rather than left as in-code judgment calls: **Decision 1** — no distinct `ParticipantReconnected` event/`Writer` field (the spec's original shape); a reconnect reuses `ParticipantJoined`, told apart from a fresh join entirely inside `applyEvent` by checking existing participant state, since per-connection writer channels live in `internal/ws`'s `hub` and were never something `RoomActor` could plug a `Writer` into anyway. **Decision 2** — a reconnect attempt *after* the grace period already expired is actively rejected (not silently allowed to rejoin fresh): `RoomActor` gained its first synchronous query, `IsEvicted(userID) bool` — a request-with-reply-channel placed on the same `inbox` as every other event, answered inside `applyEvent`, which `ws-endpoint.md`'s `ServeHTTP` calls before the WS upgrade and rejects with the same `401` as an invalid token. Implementation: `ParticipantState` gained `DisconnectedCount int` (in-memory only — deferred to `race-completion/finish-race.md`'s eventual transaction, since no batched-write mechanism this spec assumed actually exists in this codebase) and an unexported `graceTimer *time.Timer`; `RoomActor` gained an `evicted map[string]struct{}` and a package-level `var gracePeriodDuration = 30 * time.Second` (var, not const, so tests can shorten it — `withShortGracePeriod` test helper). The single new `RoomEvent` variant is `ParticipantLeft` (self-scheduled by the room actor via `time.AfterFunc` calling its own `Send`), guarded in `applyEvent` against a genuine race between an already-fired timer and an in-flight reconnect landing at nearly the same moment — only honored if the participant is still marked disconnected at apply time, caught and tested (`TestRoomActor_ApplyEvent_ParticipantLeft_StaleEventIgnoredAfterReconnect`) before it ever shipped. Also resolved a second cross-spec ambiguity found while reading `room-registry.md` again during `load`: that spec attributes empty-room teardown to this feature, but the actual mechanism (Postgres write, `race_finished` broadcast, actor/registry teardown) is `race-completion/finish-race.md`'s "zero participants remaining active" finish condition — `grace-period.md` and `finish-race.md` were both updated on disk to make that split explicit; this feature's own responsibility ends at removing the participant and marking them evicted. **Bug caught after initial implementation, before `complete`**: the first cut of `applyEvent`'s `ParticipantJoined` branch only preserved existing state when `DisconnectedAt != nil`, so a *duplicate* `join_race` from a still-connected participant (e.g. two tabs, a client retry) fell into the "unknown participant" branch and silently reset `WordsCorrect`/`LastSeq`/`DisconnectedCount` to zero mid-race — caught by directly reasoning through the condition when asked about it, not by a failing test (no test covered that path yet); fixed by widening the reuse condition to any already-known participant and narrowing the `DisconnectedAt != nil` check to only gate the reconnect-specific timer/flag cleanup, with a regression test (`TestRoomActor_ApplyEvent_ParticipantJoined_DuplicateWhileConnected_PreservesProgress`) added and confirmed to fail against the pre-fix code. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean across the whole module, `internal/room`+`internal/ws` re-run 5x with no flakiness; `internal/room` gained 10 new tests (disconnect timer/count, reconnect preserves progress, duplicate-join-while-connected, `ParticipantLeft` remove+evict, the stale-event race guard, unknown-participant no-ops, the eviction query, plus two goroutine-based real-timer tests) and `internal/ws` gained one integration test that simulates eviction via directly `Send`-ing the exported `ParticipantLeft`/`ParticipantDisconnected` events (no need to wait a real 30s) and confirms the WS handshake is rejected. Explicitly out of scope, deferred by user request to a separate future change: making `POST /races/{id}/join` idempotent for an already-joined participant while a race is still `pending` (currently returns `409 already_joined`) — unrelated to this feature's package (`internal/race`, not `internal/room`/`internal/ws`), to be scoped separately. Next feature per context/features/phase2/phase-2-plan.md: `race-completion/finish-race.md`.
- **Race Completion** (2026-07-21) — Sixth and final backend Phase 2 feature (`context/features/phase2/race-completion/finish-race.md`). The spec's `func (s *RaceService) FinishRace(...)` sketch didn't account for a real blocker, found by checking the actual dependency graph rather than the spec's code sample: `internal/race` already imports `internal/room` (`RaceHandler` holds `*room.Registry`), so a room actor calling `RaceService` directly would be `room → race` stacked on the existing `race → room` — a genuine import cycle. Resolved the same way this project already resolves Repository interfaces (coding-standards.md: "interfaces live next to their consumer"): `RaceFinisher` and its `ParticipantResult` data shape now live in `internal/room`, satisfied structurally by `RaceService.FinishRace` — no new import direction needed. Wiring stayed minimal: rather than reordering `app.go`/`route.go` (checked directly — `room.NewRegistry()` is constructed there *before* `RaceService` exists), `Registry.Spawn`/`NewRoomActor` just grew one `finisher` parameter, and `RaceHandler.Start`'s existing `registry.Spawn(...)` call passes its own `h.svc`. Also closed the empty-room `Registry` teardown gap `reconnection/grace-period.md` explicitly deferred here: `Registry.Spawn` now starts a watcher goroutine mirroring `internal/ws`'s `hubRegistry` pattern exactly, so an actor that cancels its own context (via finishing) gets cleaned out of the map even without an explicit `Remove` call. `RaceFinishedMessage`/`RaceResultJSON` moved from `internal/ws` into `internal/room` (the room actor marshals and broadcasts `race_finished` itself, same as it already does for `race_state` — the `internal/ws` copies became unused dead code the moment that was true, same precedent as `RaceStateMessage` in the WebSocket Protocol feature). Added the second "zero live participants" path the original spec missed entirely — nobody ever connecting to the live room at all, not just everyone eventually leaving — via a room-level one-shot `noShowTimeout` `time.AfterFunc`, added to both spec files and this one's `load` notes before implementation began. **Two real bugs, both caught by the existing test suite reacting correctly to new behavior, not designed for upfront**: two pre-existing tests (one in `reconnection/grace-period.md`'s own suite, one in `websocket/ws-endpoint.md`'s integration suite) each used a room with exactly one participant and asserted `IsEvicted` stayed queryable after that participant was evicted — but an empty room now correctly finishes and self-cancels, and `IsEvicted` returns `false` unconditionally once a room's context is done (its own documented contract, from the previous feature). Both fixed by adding a second, still-racing participant so eviction and finish-on-empty stay tested independently, rather than colliding. Verified: `go build`/`go vet`/`gofmt -l .`/`go test ./... -race` clean across the whole module, `internal/room`+`internal/ws` re-run 5x with no flakiness; Swagger docs regenerated (incidentally also fixed one unrelated stale-doc drift from the "Cap Race Participants at 10" feature, whose `race_full` response case had never been regenerated into `backend/docs/`). Known, disclosed gaps carried forward rather than silently built past scope: no retry if the Postgres finish transaction fails (logged, room stays running rather than torn down — no retry/backoff story exists anywhere in this codebase); `AvgPaceWatt` always writes `0.0` since nothing tracks per-tick pace anywhere (`internal/ws`'s `ClientMessage.PaceWatt` is decoded off the wire but was never forwarded into `TelemetryReceived` by any prior feature). This closes out Phase 2's backend work entirely — per `context/features/phase2/phase-2-plan.md`, only `frontend-realtime/websocket-client.md` and `frontend-realtime/reconnect-ui.md` remain, both frontend-only.
- **Leave Race** (2026-07-21) — Not part of the original Phase 2 roadmap, added by explicit request after the rest of Phase 2's backend was done (`context/features/phase2/leave-race/leave-race.md`). Renamed the existing grace-period-timeout `RoomEvent` from `ParticipantLeft` to `ParticipantEvicted` (chosen over the user's suggested `ParticipantDisconnectedPermanently` since it reuses vocabulary — `evicted`/`IsEvicted` — already established in shipped code) as an isolated first step, verified green before layering new behavior on top; freed the `ParticipantLeft` name for this feature's new intentional-quit event, deliberately kept as a distinct event type rather than a flag on `ParticipantEvicted` (the two differ in more than just their guard: an intentional quit is always honored, no `DisconnectedAt` check). Added `POST /races/{id}/leave` (`pending`-only, real `DELETE FROM race_participants`, new `ErrNotParticipant` sentinel) through the standard `internal/race` Handler/Service/Repository layers, and a `leave_race` WebSocket message wired through `internal/ws/protocol.go` into the new `ParticipantLeft` event — `internal/ws`'s reader loop now closes the connection right after sending the event, rather than waiting for the client to hang up on its own. Rank assignment changed per explicit user spec ("a 5-player race where everyone quits mid-race means every one of them is recorded at rank 5"): both `ParticipantEvicted` and `ParticipantLeft` now move the departing participant into a new `departedParticipants` map (via a shared `departParticipant` helper) instead of a bare `delete()`, and `checkRaceFinished` unions `participants` + `departedParticipants` when building results — any entry without a `FinishRank` gets one shared value equal to `totalParticipants` (a counter incremented only on a genuinely new join, never a reconnect or duplicate-while-connected join). Flagged and accepted a real behavior change to already-shipped `finish-race.md`: an abandoned race (everyone quits, nobody finishes) previously produced an *empty* results slice; it now produces a full result set, everyone who ever joined tied at the bottom rank. **Real bug caught during user review, before `complete`**: `TelemetryReceived`'s per-finish rank was computed by counting `FinishRank != nil` over the *live* `participants` map at the moment someone finished — so a participant who already finished and *then* departed (their connection dropped and the grace period lapsed) silently dropped out of that count, handing the next finisher a **duplicate rank** (e.g. two participants both recorded at rank 1). Fixed by replacing the live count with a monotonic `finishedCount int` field on `RoomActor`, incremented once per finish and immune to who's since departed; added a regression test reproducing the exact collision (finish → disconnect → grace-period-evict → next finisher), confirmed it failed against the old loop-based code before the fix and passed after. Verified: `go build`/`go vet`/`gofmt -l .` clean, full `go test ./... -race` green, `internal/room`+`internal/ws` re-run 5x with no flakiness (both before and after the rank-collision fix), Swagger docs regenerated including the new `/races/{id}/leave` endpoint. Per `context/features/phase2/phase-2-plan.md`, only `frontend-realtime/websocket-client.md` and `frontend-realtime/reconnect-ui.md` remain — both frontend-only, and now the last two items in Phase 2.
